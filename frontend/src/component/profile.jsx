import React from "react";
import Frame from "../layout/frame";
import FixedProfile from "../layout/fixedProfile";

export const Profile = () => {
    return (
        <Frame>
            <FixedProfile />
        </Frame>
    );
};